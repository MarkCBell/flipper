
import Flipper

class SplittingSequence:
	def __init__(self, initial_lamination, preperiodic, periodic, isometries):
		self.sequence = sequence
		self.isometries = isometries
	def build_bundle(isometry):
		L = Flipper.LayeredTriangulation()
		
		return 
